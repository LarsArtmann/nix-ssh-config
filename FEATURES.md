# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

## SSH client (Home Manager module)

| Feature                                                    | Status                | Notes                                                                                                                                                                                     |
| ---------------------------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Per-host match blocks with full sub-options                | 🟢 `FULLY_FUNCTIONAL` | `modules/home-manager/ssh.nix`; host `user` inherits `ssh-config.user`; host `certificateFile`/`controlMaster`/`updateHostKeys` supported; asserted by `hm-host-blocks`/`hm-host-options` |
| Hardened global defaults (`*` block)                       | 🟢 `FULLY_FUNCTIONAL` | `modules/home-manager/ssh.nix`; agent forwarding off, keepalives, crypto profile — asserted by `hm-global-defaults`; effective settings runtime-proven via `ssh -G` (`hm-ssh-g-preview`)  |
| GitHub.com preset (connection pooling, git user)           | 🟢 `FULLY_FUNCTIONAL` | asserted by `hm-github-preset`                                                                                                                                                            |
| `proxyJump`, `forwardX11`, forwards (local/remote/dynamic) | 🟢 `FULLY_FUNCTIONAL` | Structured values passed to HM's native renderer; asserted by `hm-host-advanced`; rendered config verified end-to-end                                                                     |
| OrbStack / Colima includes (Darwin)                        | 🟢 `FULLY_FUNCTIONAL` | conditional on `builtins.pathExists`                                                                                                                                                      |
| `~/.ssh/sockets` activation script (mode 700)              | 🟢 `FULLY_FUNCTIONAL` |                                                                                                                                                                                           |
| Extra config includes (`extraIncludes`)                    | 🟢 `FULLY_FUNCTIONAL` |                                                                                                                                                                                           |

## SSH server (NixOS module)

| Feature                                              | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hardened sshd defaults (keys-only, no root)          | 🟢 `FULLY_FUNCTIONAL` | asserted by `nixos-password-auth-disabled`, `nixos-root-login-disabled` and VM `sshd -T` subtests                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Keys-only closes keyboard-interactive                | 🟢 `FULLY_FUNCTIONAL` | `kbdInteractiveAuthentication` defaults to `passwordAuthentication` (NixOS default `yes` + `UsePAM` would keep PAM prompts — OTP modules, or Unix passwords where the PAM service permits — available with passwords off, issue #1); independently settable for PAM 2FA (which also forces `security.pam.services.sshd.unixAuth = true`: upstream couples that flag to `PasswordAuthentication` and would otherwise `pam_deny` every prompt); asserted by `nixos-kbd-interactive`, `nixos-password-auth-disabled` and four VM subtests |
| Global authorized keys (`/etc/ssh/authorized_keys`)  | 🟢 `FULLY_FUNCTIONAL` | **copied** into /etc (`mode = "0444"`), not symlinked — symlinks are rejected at runtime by sshd StrictModes; proven by VM key-login subtest (this was broken before 2026-08-22)                                                                                                                                                                                                                                                                                                                                                       |
| Legal banner (overridable, nullable)                 | 🟢 `FULLY_FUNCTIONAL` | path uses `lib.mkDefault`; control-char rejection via module assertion; asserted by `nixos-banner` + VM                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `LoginGraceTime` pinned to 30s                       | 🟢 `FULLY_FUNCTIONAL` | explicit hardened default (upstream 120s + jitter since 9.9); overridable via `extraSettings`; snapshot-guarded by the VM golden                                                                                                                                                                                                                                                                                                                                                                                                       |
| `listenAddresses` (bind specific `addr:port` pairs)  | 🟢 `FULLY_FUNCTIONAL` | emitted as ListenAddress list; asserted by `nixos-custom-settings`                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `usePam` / `authenticationMethods` (2FA first-class) | 🟢 `FULLY_FUNCTIONAL` | `nullOr` passthrough into `settings.UsePAM`/`AuthenticationMethods`; null leaves NixOS defaults; asserted by `nixos-custom-settings`                                                                                                                                                                                                                                                                                                                                                                                                   |
| `MaxStartups`/`PerSourcePenalties` explicit defaults | 🟢 `FULLY_FUNCTIONAL` | MaxStartups tightened to `10:30:60`; penalties explicitly on (upstream default since 9.8 pinned); asserted by `nixos-custom-settings`, snapshot-guarded                                                                                                                                                                                                                                                                                                                                                                                |
| `sshd -T` golden snapshot                            | 🟢 `FULLY_FUNCTIONAL` | `tests/sshd-t-golden.txt` pins the effective sshd output for every module-controlled directive; VM subtest fails with a key-by-key diff on runtime drift                                                                                                                                                                                                                                                                                                                                                                               |
| User allow-list + port config                        | 🟢 `FULLY_FUNCTIONAL` | `types.port`, firewall opened; port wiring asserted by `nixos-custom-settings`                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `extraSettings` escape hatch                         | 🟢 `FULLY_FUNCTIONAL` | typed `attrsOf (oneOf [str int bool])`, merges last (can override any default); asserted by `nixos-custom-settings`                                                                                                                                                                                                                                                                                                                                                                                                                    |

## Crypto profile (shared)

| Feature                                        | Status                | Notes                                                          |
| ---------------------------------------------- | --------------------- | -------------------------------------------------------------- |
| Post-quantum KEX (ML-KEM hybrid first)         | 🟢 `FULLY_FUNCTIONAL` | `modules/shared/crypto.nix:4-9`; used by both modules          |
| AEAD-only ciphers                              | 🟢 `FULLY_FUNCTIONAL` | `modules/shared/crypto.nix:11-15`                              |
| Encrypt-then-MAC MACs only                     | 🟢 `FULLY_FUNCTIONAL` | `modules/shared/crypto.nix:17-21`                              |
| Modern host key algorithms (Ed25519, RSA-SHA2) | 🟢 `FULLY_FUNCTIONAL` | `modules/shared/crypto.nix:23-28`                              |
| ML-DSA post-quantum signatures                 | ⚪ `PLANNED`          | No OpenSSH implementation exists; watch upstream (see ROADMAP) |

## Flake & CI

| Feature                                       | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `homeManagerModules.ssh` / `nixosModules.ssh` | 🟢 `FULLY_FUNCTIONAL` | evaluated + content-asserted by checks on 3 systems                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `examples.client` / `examples.server` outputs | 🟢 `FULLY_FUNCTIONAL` | imported as real modules by `examples-evaluate` on every system                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `sshKeys` output (tracked pubkeys)            | 🟢 `FULLY_FUNCTIONAL` | `lars`, `lars-evo-x2`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Evaluation + content checks                   | 🟢 `FULLY_FUNCTIONAL` | 20 eval/content checks per system (Linux: 21, assertion checks are Linux-only; x86_64-linux adds the VM test) + formatters; all Nix attribute-equality (`assertEq`), each family kill-switch tested; `docs-option-inventory`/`docs-check-count` guard README option tables and these counts against drift                                                                                                                                                                                                                    |
| VM integration test (QEMU)                    | 🟢 `FULLY_FUNCTIONAL` | `checks.x86_64-linux.nixos-vm-sshd` — boots VM, `sshd -T` runtime assertions, real key login; runtime-proves negotiated PQ KEX (`ssh -vv`), `ssh -Q` support of every configured algorithm, wrong-key rejection (publickey-only method list), banner delivery, and the prompt path end-to-end (`sshpass` kbd-interactive exchange on a locked-user variant node, refused outright on the hardened default); caught the global-keys StrictModes bug on first run and the PAM `unixAuth` coupling bug via its positive control |
| Formatting via treefmt-nix                    | 🟢 `FULLY_FUNCTIONAL` | runs as `checks.format` and `nix fmt`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| GitHub Actions CI                             | 🟢 `FULLY_FUNCTIONAL` | x86_64 job (checks + link check) and native aarch64-linux job; green on GitHub                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Markdown link checking                        | 🟢 `FULLY_FUNCTIONAL` | lychee in CI over all living docs and examples                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Dev shell (`nix develop`)                     | 🟢 `FULLY_FUNCTIONAL` | provides `nil` (formatting handled by treefmt)                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
